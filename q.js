let double=(a)=>{
    let pera=document.querySelector("#pera")
    // pera.innerHTML=a*a;
    // pera.innerHTML=`the square of ${a} is ${a*a}`;
    pera.innerHTML=`
    square is ${a*a} <br>
    cube is ${a*a*a}     <br>
    Even or odd is
    if(a%2==0){
    }     `
}